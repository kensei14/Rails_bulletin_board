<h1>Listing boards</h1>

<table>
  <tr>
    <th>User_Name</th>
    <th>Title</th>
    <th>Comment</th>
    <th>D date</th>
    <!--<th>D time</th>-->
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @boards.each do |board| %>
  <tr>
    <td><%= link_to board.id, {:controller => "users", :action => "show", :id => board.id, :name => "test"} %></td>
    <td><a href=<%= url_for :controller => "users", :action => "show", :id => board.id %>>aaaa</a>
    <td><%= board.title %></td>
    <td><%= board.comment %></td>
    <td><%= board.d_date %></td>
    <td><%= board.d_time %></td>
    <td><%= link_to 'Show', board %></td>
    <td><%= link_to 'Edit', edit_board_path(board) %></td>
    <td><%= link_to 'Destroy', board, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    <!--
    <td><%# board.id %></td>
    <td><%# board.user_id %></td>
    <td><%# url_for :controller => "users", :action => "show", :id => board.id %></td>
    <td><%# link_to @user.id.to_s(), {:controller => "users", :action => "show", :id => board.id, :name => "test"} %></td>
    -->
  </tr>
<% end %>
</table>

<br />

<%= form_tag(:controller=>"boards", :action=>"create") do -%>
  <div class="user_wrapper">
    <h2>ユーザーID</h2>
    <%= text_field(:contribute, :user_id, :class=>"user_text", :size=>"60") %>
    <h2>投稿タイトル</h2>
    <%= text_field(:contribute, :title, :class=>"user_text", :size=>"60") %>
    <br/>
    <h2>投稿内容</h2>
    <%= text_area(:contribute, :comment, :class=>"user_area", :size=>"60x20") %>
    <br/>
    <%= submit_tag("送信", :confirm=>"Are you sure?", :size=>"50") %>
    <br/>
  </div>
<% end -%>

<table>
  <tr>
    <th>User_Id</th>
    <th>Name</th>
    <th>Description</th>
  </tr>
<% @users.each do |user| %>
  <tr>
    atd><%= user.user_id %></td>
    <td><%= user.name %></td>
    <td><%= user.description %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_board_path(user) %></td>
    <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<%= link_to 'New Board', new_board_path %>
